apiVersion: v1
kind: Service
metadata:
  name: {{ include "seldon-mcp.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "seldon-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "seldon-mcp.selectorLabels" . | nindent 4 }}
